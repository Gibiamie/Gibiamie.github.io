name: MIC insider conviction data

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - scripts/fetch_mic_insiders.py
      - .github/workflows/mic-insider.yml
  schedule:
    - cron: '*/30 * * * 1-5'

permissions:
  contents: write

concurrency:
  group: mic-insider-data
  cancel-in-progress: true

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      SEC_USER_AGENT: MIC Insider Scanner/1.0 100677393+Gibiamie@users.noreply.github.com
      MIC_SEC_PAGES: '2'
      MIC_MAX_NEW_FORM4: '120'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install market-data dependencies
        run: python -m pip install --disable-pip-version-check requests yfinance
      - name: Scan SEC Form 4 purchases and enrich market data
        run: python scripts/fetch_mic_insiders.py
      - name: Commit changed scanner data
        run: |
          if git diff --quiet -- mic/data/insider; then
            echo "No insider scanner changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add mic/data/insider
          git commit -m "data: refresh MIC insider conviction scanner"
          git push

# v35 initial production trigger
